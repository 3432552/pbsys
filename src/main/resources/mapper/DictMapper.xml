<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hzy.manager.dao.DictMapper">
    <select id="getDictInfoByCondition" resultType="com.hzy.manager.domain.Dict">
        select * from p_dict
        limit #{currentNo},#{pageSize}
    </select>
</mapper>
